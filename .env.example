# ===========================================================================
# Coodra — Environment Variables (example)
# ---------------------------------------------------------------------------
# Copy this file to `.env` and fill in real values for your environment.
# Never commit `.env`. Dev values below are solo-mode safe (local loopback).
# Required-at-startup vars are validated by packages/shared/src/config.ts
# via Zod; a service that needs a missing required var fails fast.
# ===========================================================================

# --- Mode ------------------------------------------------------------------
# Controls auth/sync behavior. Local services use SQLite as the primary store;
# team mode adds identity and Postgres sync.
# Values: "solo" | "team". Default: "solo".
COODRA_MODE=solo

# --- Logging ---------------------------------------------------------------
# pino log level. Values: "debug" | "info" | "warn" | "error". Default: "info".
LOG_LEVEL=info

# --- Service ports (local dev) --------------------------------------------
MCP_SERVER_PORT=3100
WEB_APP_PORT=3001

# --- SQLite (solo mode) ----------------------------------------------------
# Path to the local SQLite file. Default: ~/.coodra/data.db (expanded at
# runtime). Override only for test isolation.
# COODRA_SQLITE_PATH=

# --- MCP server local-state roots ------------------------------------------
# Override the on-disk root for context-pack materialisation when needed.
# COODRA_CONTEXT_PACKS_ROOT=

# --- Postgres (team mode) --------------------------------------------------
# Supabase / managed Postgres with pgvector extension enabled.
# Required when COODRA_MODE=team.
# DATABASE_URL=postgres://postgres:postgres@localhost:5432/coodra

# --- Redis (team mode) -----------------------------------------------------
# Upstash or compatible Redis URL. Required when COODRA_MODE=team.
# REDIS_URL=redis://localhost:6379

# --- Auth (Clerk) ----------------------------------------------------------
# Solo-mode bypass is honoured explicitly by the auth middleware. Replace with
# real Clerk values before enabling team-mode auth.
CLERK_SECRET_KEY=sk_test_replace_me
CLERK_PUBLISHABLE_KEY=pk_test_replace_me

# --- Local lifecycle secret (team mode) ------------------------------------
# Generated by team setup/join flows where needed. Do NOT reuse across
# machines. Do NOT commit.
# LOCAL_HOOK_SECRET=

# Clerk JWT issuer (team mode) — the iss claim your Clerk instance stamps.
CLERK_JWT_ISSUER=

# HMAC secret that signs team invite tokens (coodra invite / web install links).
COODRA_INVITE_HMAC_SECRET=

# --- GitHub integration ----------------------------------------------------
# Optional integration credentials. Never commit the PEM; store the path or the
# secret-manager handle here.
# GITHUB_APP_ID=
# GITHUB_CLIENT_ID=
# GITHUB_CLIENT_SECRET=
# GITHUB_WEBHOOK_SECRET=
# GITHUB_PRIVATE_KEY_PATH=

# --- Atlassian / JIRA integration -----------------------------------------
# Optional Atlassian OAuth 2.0 (3LO) app credentials.
# ATLASSIAN_CLIENT_ID=
# ATLASSIAN_CLIENT_SECRET=
# ATLASSIAN_WEBHOOK_SECRET=

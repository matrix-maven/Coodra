// CSS module declarations — Next.js's `next-env.d.ts` covers most of
// these but we ship our own to be explicit about side-effect imports
// of plain .css files (e.g. `import './globals.css'` in app/layout.tsx).

declare module '*.css';

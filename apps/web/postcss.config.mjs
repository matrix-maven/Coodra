/**
 * Tailwind v4 CSS-first config — the entire token catalog lives in
 * `app/globals.css` under `@theme`, sourced verbatim from the brand
 * tokens at `docs/brand/brand.html`. No `tailwind.config.ts` needed.
 */
export default {
  plugins: {
    '@tailwindcss/postcss': {},
  },
};

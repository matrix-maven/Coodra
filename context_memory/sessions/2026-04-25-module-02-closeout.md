# Current Session — 2026-04-22 (Module 02)

## Goal

Land Module 02 (MCP Server) end-to-end per `docs/feature-packs/02-mcp-server/`: two-transport MCP server at `apps/mcp-server/`, tool-registration framework (three-file-per-tool), eight `coodra__*` tools, Policy Engine with `policies` + `policy_rules` + `policy_decisions`, Feature Pack service, Context Pack service (Unicode-safe excerpt), Run Recorder (outbox), sqlite-vec wiring, Clerk + solo-bypass + `LOCAL_HOOK_SECRET` auth chain on HTTP transport, §24.9 manifest test, documented graceful fallbacks for `search_packs_nl` and `query_codebase_graph`. Push `feat/02-mcp-server` to `https://github.com/Abishai95141/Coodra`; user reloads IDE post-merge to make `coodra__*` tools callable for the first time.

## Context loaded

- `essentialsforclaude/05-agent-trigger-contract.md` (non-negotiable; we are now building the tools this file describes).
- `essentialsforclaude/09-common-patterns.md §9.1` (three-file-per-tool pattern is the required layout).
- `system-architecture.md` §3.5 (stdio + Streamable HTTP transports simultaneously), §4.3 (append-only tables + idempotency keys), §5 Policy Evaluation → AP, §7 Fail-Open, §16 (design patterns 1/3/4/9/19 — CQRS, Outbox, Fail-open, Feature Pack inheritance, Tool descriptions are agent prompts), §19 (Clerk JWT + local secret + solo-bypass), §24 (full MCP Tool Manifest & Agent Discovery Contract — §24.3 description recipe, §24.4 core tool descriptions verbatim, §24.7 file layout, §24.8 safeguards, §24.9 manifest test).
- `External api and library reference.md` → Protocols & Transports (MCP + Streamable HTTP), Validation/Schemas/Resilience (Zod + zod-to-json-schema + cockatiel), Auth & Security (Clerk), Databases (sqlite-vec gotchas).
- Prior context packs consulted: `docs/context-packs/2026-04-22-module-01-foundation.md` (the handoff from Module 01). Archive now has one entry: `context_memory/sessions/2026-04-22-module-01.md`.
- User's Module 02 directive with approved plan + answers to Q-02-1 through Q-02-7 + additions A/B/C/D.

## Last completed

[16:35] S5 implementation complete and pre-commit-verification green. Net-new `apps/mcp-server` workspace: walking skeleton with stdio transport + tool-registration framework + `ping` tool. `@modelcontextprotocol/sdk@1.29.0` pinned exact (no caret). Four S5 user constraints honoured end-to-end: (1) zod-validated typed `env` singleton with fail-fast at module load, no `process.env.X!` outside `src/config/env.ts`; (2) tool-registration framework enforces six invariants synchronously at register time (name shape, ≥200-char description, z.object input, Zod output, arity-2 handler, idempotency-key builder returns well-formed key) — invalid registrations throw and handlers cannot opt out of the automatic pre+post policy wrapper; (3) multi-stage Dockerfile via `pnpm deploy --prod --legacy`, base image `node:22.16.0-bookworm-slim` pinned by digest `048ed02c...0e34` (resolved on host, glibc per user's anti-musl directive); (4) `ping` tool lands as walking-skeleton proof — 666-char description, readonly idempotency-key builder, round-trips `serverTime/sessionId/idempotencyKey/echo`. Stderr-logging contract landed in three layers: shared logger reads `COODRA_LOG_DESTINATION` at module load and routes pino to fd 2 when set to `stderr` (unknown values throw); `src/bootstrap/ensure-stderr-logging.ts` imported first in `src/index.ts` sets the env before `@coodra/shared` evaluates; Dockerfile + `.mcp.json` set the env as defence-in-depth. Subprocess stdout-purity test asserts every stdout byte is JSON-RPC and every stderr line is pino-JSON. 34/34 mcp-server unit tests green. Shared bumped to 64/64 (was 60, +4 destination-contract). `zod-to-json-schema` dropped permanently in favour of Zod v4's native `z.toJSONSchema()`. `External api and library reference.md` gains a full `@modelcontextprotocol/sdk` subsection + Pino `COODRA_LOG_DESTINATION` gotcha (amendment B). Five S5 decisions appended to `decisions-log.md` (stdio-only scope, log destination contract, Server-not-McpServer, drop zod-to-json-schema, Dockerfile digest pin).

## Next action

Commit S5: `feat(mcp-server): scaffold @coodra/mcp-server — stdio transport, tool-registration framework, ping walking skeleton`. One commit per amendment B. Then **pause for user review** — S6 is the now-small residual §24.3 assertion helper + system-architecture.md §24.3 amendment; S7a/b/c start the lib/ directory (db, env-extensions, auth, policy, feature-pack, context-pack, run-recorder, graphify, sqlite-vec-client) which is the next substantial slice.

## Log (append-only per PostToolUse)

- [20:55] branch `feat/02-mcp-server` created from `main` at `88aac10`
- [20:58] wrote `docs/feature-packs/02-mcp-server/spec.md` (133 lines) — scope, 21 AC, non-goals, four-table schema (policies/policy_rules/policy_decisions/feature_packs) + `content_excerpt` column, transport contract, fail-open discipline
- [20:59] wrote `docs/feature-packs/02-mcp-server/implementation.md` (324 lines) — 23-slice plan (S1..S21 with S7a/S7b/S7c per addition A), reference-update mapping per amendment B, migration lock protection per addition B, env-schema strictness per addition C, env regression test per addition D
- [21:00] wrote `docs/feature-packs/02-mcp-server/techstack.md` (85 lines) — Module-02 deps pinned: @modelcontextprotocol/sdk ^1.29.0, hono ^4.12.14, @hono/node-server ^2.0.0, cockatiel ^3.2.1, zod-to-json-schema ^3.25.2, @clerk/backend ^3.2.13, ajv ^8.18.0, ajv-formats ^3.0.1, testcontainers ^11.14.0, @testcontainers/postgresql ^11.14.0, sqlite-vec ^0.1.9; gotchas list extended
- [21:02] commit `19ded3f` on `feat/02-mcp-server` — `docs(02-mcp-server): spec, implementation plan, techstack` (3 files, +542)
- [21:06] copied `context_memory/current-session.md` → `context_memory/sessions/2026-04-22-module-01.md` and rewrote the archive header to mark it archived (module completed, main at 88aac10)
- [21:07] rewrote `context_memory/current-session.md` for Module 02 (this file)
- [21:08] appended 11 decisions to `context_memory/decisions-log.md` — Q-02-1 auth chain (3 layers, first match); Q-02-2 async idempotent policy_decisions writes (WARN on failure); Q-02-3 Unicode code-point-safe content_excerpt; Q-02-4 Feature Pack FS source of truth + checksum invalidation; Q-02-5 Clerk wired-but-unvalidated; Q-02-6 manifest word budget 40-80 soft / 120 hard, §24.3 amendment; Q-02-7 .mcp.json workspace-relative; Addition A S7 split into S7a/S7b/S7c; Addition B hand-edited migrations sha256-locked; Addition C env schema strict on Clerk; Addition D env fixtures regression test
- [21:09] edited `context_memory/pending-user-actions.md` — Docker DUE NOW (AC-5 at S17); Clerk re-scoped to Module 04 OR first team-mode flip (not blocking Module 02 merge); GitHub App + Atlassian OAuth re-scoped to post-Module-02 integration module
- [21:10] commit `8525ca9` on `feat/02-mcp-server` — `chore(context-memory): archive module-01 session, begin module-02` (4 files, +217/-106)
- [21:25] edited `packages/db/src/schema/sqlite.ts` — refreshed docblock for 9-table total; added `content_excerpt` column (`text notNull default('')`) to `contextPacks` after `content`; appended `policies`, `policyRules` (indexed on `policy_id, priority`), `policyDecisions` (indexed on `session_id, created_at` + unique on `idempotency_key`, `run_id` nullable so PreToolUse before a run exists can still record), `featurePacks` (unique slug). `isActive` uses `integer({ mode: 'boolean' })` so Drizzle reports `dataType: 'boolean'` — parity with Postgres's native `boolean`.
- [21:26] edited `packages/db/src/schema/postgres.ts` — same additions, docblock refreshed; imported `boolean` from `pg-core`; native `boolean` + `timestamp` primitives throughout; summary_embedding stays `vector(384)` (architected dialect drift still on the exemption list).
- [21:27] edited `packages/db/__tests__/unit/schema-parity.test.ts` — extended `tablePairs` from 5 → 9 entries (policies, policy_rules, policy_decisions, feature_packs added); renamed describe from "5-table core is present" to "nine-table schema is present" and added per-table presence assertions for the four new tables; docblock updated to record the surviving architectural exemption (context_packs.summary_embedding). Exemption list unchanged (no new exemptions).
- [21:28] ran `pnpm --filter @coodra/db test:unit` — 42/42 green (up from 30/30: +12 tests for 4 new tables × 3 checks each = column-name parity + notNull parity + dataType parity). Confirms `integer({ mode: 'boolean' })` vs native `boolean` reports identical Drizzle `dataType`.
- [21:29] ran `pnpm --filter @coodra/db db:generate` — drizzle-kit emitted `drizzle/sqlite/0001_chief_turbo.sql` + `drizzle/postgres/0001_clean_rafael_vega.sql` + two `meta/0001_snapshot.json` files; both `_journal.json` files updated. Per-dialect summary: 9 tables, same column counts. (SQLite reports `.unique()` as an extra index; Postgres reports it as an inline constraint — same column semantics, different Drizzle-Kit bookkeeping.)
- [21:30] audited both 0001 migrations manually — SQLite: 4 CREATE TABLE + 2 CREATE INDEX + 2 CREATE UNIQUE INDEX + ALTER TABLE context_packs ADD content_excerpt. Postgres: 4 CREATE TABLE with inline UNIQUE constraints + 5 ALTER TABLE ADD CONSTRAINT FK + 2 CREATE INDEX + ALTER TABLE context_packs ADD COLUMN. Both produce the nine-table, FK-correct schema.
- [21:42] first pnpm test:unit run after migration generation: 2 failures in `client.test.ts` — the Module-01 assertions `expect(tables).toEqual(['context_packs','pending_jobs','projects','run_events','runs'])` and `expect(rows.n).toBe(5)` were pinned to the 5-table core.
- [21:43] edited `packages/db/__tests__/unit/client.test.ts` — renamed the test from "creates the 5-table core" to "creates the nine-table schema"; lifted the table-list expectation to the sorted nine-table list; bumped the idempotency-test table count from `5` to `9`. Not a test weakening — it re-pins the assertion to the current reality, locks both the new tables and the add-column migration, and will now catch future drift toward a different count (9 → N).
- [21:45] reran `pnpm lint && pnpm typecheck && pnpm test:unit` — all green; 42/42 tests across @coodra/shared + @coodra/db.
- [21:48] commit `533934b` on `feat/02-mcp-server` — `feat(db): policies, policy_rules, policy_decisions, feature_packs tables` (11 files, +2151/-30).
- [22:00] wrote `packages/shared/src/constants.ts` — `EMBEDDING_DIM = 384` as `const` with a five-step change-checklist docblock; `EmbeddingDim` type alias of the literal; re-exported from `packages/shared/src/index.ts` + added `./constants` subpath export in `packages/shared/package.json` so downstream consumers can import without pulling the logger/pino tree.
- [22:01] wrote `packages/shared/__tests__/unit/constants.test.ts` — two assertions (runtime value + compile-time literal lock); 60/60 shared tests pass.
- [22:05] `pnpm --filter @coodra/db add sqlite-vec@0.1.9 --save-exact` — installed as exact pin; then moved from `devDependencies` to `dependencies` because `createSqliteDb` calls `sqliteVec.load` at runtime. Platform binary `sqlite-vec-darwin-arm64@0.1.9` resolved via optionalDependencies.
- [22:06] added `"check:migration-lock": "node scripts/check-migration-lock.mjs"` to `packages/db/package.json` scripts.
- [22:10] hand-appended the sqlite-vec preserve-block to `packages/db/drizzle/sqlite/0001_chief_turbo.sql` — `CREATE VIRTUAL TABLE IF NOT EXISTS context_packs_vec USING vec0(context_pack_id TEXT PRIMARY KEY, embedding FLOAT[384])` wrapped in `-- @preserve-begin hand-written:sqlite-vec` / `-- @preserve-end`.
- [22:11] hand-appended the pgvector HNSW preserve-block to `packages/db/drizzle/postgres/0001_clean_rafael_vega.sql` — `CREATE EXTENSION IF NOT EXISTS vector;` + `CREATE INDEX IF NOT EXISTS context_packs_embedding_hnsw_idx ON context_packs USING hnsw (summary_embedding vector_cosine_ops) WITH (m=16, ef_construction=64)` wrapped in `-- @preserve-begin hand-written:pgvector-hnsw`.
- [22:20] wrote `packages/db/scripts/check-migration-lock.mjs` (≈210 lines, plain .mjs, no @ts-check) — walks `drizzle/{sqlite,postgres}/*.sql`, extracts preserve-blocks, computes sha256 of block bodies (exclusive of sentinels), diffs against `migrations.lock.json`, exits non-zero with a diffable message naming file + marker + expected/current sha256 + lineRange + remediation command. Three labelled failure modes: `MISSING_IN_FILE`, `MISSING_IN_LOCK`, `SHA256_MISMATCH`. `--write` mode preserves `generatedAt` for blocks whose sha256 is unchanged so running `--write` twice is idempotent (important so CI can use `--write && git diff --exit-code` as a self-check if ever needed).
- [22:22] generated `packages/db/migrations.lock.json` with 2 entries (postgres-hnsw + sqlite-vec blocks), each carrying `{ file, blockMarker, sha256, lineRange, generatedAt }` per the S4 refinement.
- [22:24] smoke-tested drift — mutated `FLOAT[384]` → `FLOAT[512]` in the sqlite block, ran the checker, got the expected `SHA256_MISMATCH` output with all diagnostic fields present + exit code 1; restored and re-verified `ok (2 blocks verified)`.
- [22:30] edited `packages/db/src/client.ts` — added `import * as sqliteVec from 'sqlite-vec'`; introduced `const clientLogger = createLogger('db.sqlite-vec-loader')`; added `CreateSqliteDbOptions.loadVecExtension?: boolean` (default true); wrote `vecLoadIsRequired()` (reads `NODE_ENV` + `COODRA_REQUIRE_VEC` on every call so tests can flip env at runtime); wrote `export function loadSqliteVecOrFail(raw)` that throws `InternalError('sqlite_vec_unavailable')` under strict env and WARN-logs under relaxed env (structured fields: `event`, `loadablePath`, `platform`, `arch`, `err`); wired it into `createSqliteDb` between pragma setup and Drizzle wrap, closing the raw handle on throw. Fixed `InternalError` call shape (second positional arg is `cause`, not `{ cause }`).
- [22:34] updated `packages/db/__tests__/unit/client.test.ts` — migration 0001 now creates 10 logical objects (9 Drizzle tables + `context_packs_vec` virtual table) plus ~5 vec0 shadow tables. Extended both assertions to filter shadow tables via `substr(name, 1, 18) <> 'context_packs_vec_'` and lift the expected list/count from 9 → 10. Test explicitly locks `context_packs_vec` so drift in the hand-written block fails here as a belt-and-braces complement to the sha256 check.
- [22:37] wrote `packages/db/__tests__/integration/sqlite-vec.test.ts` — two describe blocks: (1) `sqlite-vec loadable extension + vec0 virtual table`: asserts `vec_version()` succeeds, asserts the vtab DDL in sqlite_master matches `USING vec0` + `FLOAT[${EMBEDDING_DIM}]` (so a drift between the shared constant and the migration fails here), round-trips two 384-element JSON-text vectors and asserts KNN-MATCH orders target ahead of the orthogonal vector with distance ~0; (2) `loadSqliteVecOrFail failure modes`: stub-injects a throwing `loadExtension`, toggles env, asserts strict-throw under `NODE_ENV=test`, strict-throw under `COODRA_REQUIRE_VEC=1` with `NODE_ENV=production`, and fail-open under both relaxed. First run failed because `EMBEDDING_DIM` was `undefined` at import time — shared dist was stale; fixed by `pnpm --filter @coodra/shared build`. Second run failed with "zero-length vectors are not supported" on the BLOB insert path; fixed by using the canonical JSON-text vector form (`[${values.join(',')}]`) that sqlite-vec accepts directly, sidestepping any better-sqlite3 typed-array binding ambiguity. 6/6 green.
- [22:42] extended `packages/db/__tests__/integration/postgres-migrate.test.ts` — `SCHEMA_TABLES` constant lists all 9 logical tables; beforeAll drops them in FK dependency order; `creates the nine-table schema` asserts the full list; new assertion `context_packs.content_excerpt is a non-null text column with default empty string`; new assertion `policy_rules has the (policy_id, priority) btree index`; new assertion `hand-written block: context_packs.summary_embedding has an HNSW index with m=16, ef_construction=64` — reads `pg_indexes.indexdef` and regex-matches `USING hnsw`, `vector_cosine_ops`, and both WITH params (complements the sha256 check: even if the lock file were tampered, the live DB shape has to match). Fixed postgres.js array parameter syntax (native tagged-template array binding, not `handle.raw.array(...)`). Suite is skipped locally (no DATABASE_URL) and run in CI via the existing `integration` job.
- [22:48] wrote `.github/workflows/ci.yml` change — added `Migration lock (hand-written preserve-blocks in drizzle/*.sql)` step to the `verify` job, placed **before** lint so a drift surfaces as the first failure signal. Invokes `pnpm --filter @coodra/db run check:migration-lock`.
- [22:49] wrote `.githooks/pre-commit` (chmod +x) — zero-dependency shell script; checks `git diff --cached --name-only` for staged changes under `packages/db/` and runs `pnpm --filter @coodra/db run check:migration-lock` only when that directory is touched (commits in other packages stay fast). CI always runs the check, so a skipped hook can never land drift. Guarded against missing `pnpm` with a WARN + exit 0 so non-dev checkouts don't fail.
- [22:50] added `"prepare": "git config core.hooksPath .githooks 2>/dev/null || true"` to root `package.json` scripts. `pnpm install` runs `prepare`, so every contributor gets the hook wired on first install. `|| true` guards against non-git checkouts (e.g. a Docker COPY without .git) so the install itself never fails.
- [22:52] ran `git config core.hooksPath .githooks` directly so the hook is live for THIS session's S4 commit. Smoke-tested it with a fake drift + staged SQL file — hook fired, produced the full `SHA256_MISMATCH` diagnostic, and exit 1 (commit would have been blocked). Restored + unstaged.
- [22:55] rewrote the `sqlite-vec` section of `External api and library reference.md` — replaced the speculative pre-Module-01 content with Module-02 ground truth: pinned exact 0.1.9 (no caret) rationale, pnpm exact-add command, Node load pattern, actual `vec0` DDL we emit, the fact that 0.1.9 does NOT accept `distance_metric=cosine` inline (only default L2; cosine is available via `vec_distance_cosine()`), JSON-text vector input pattern, the strict-vs-WARN load contract (with env names + rationale), five gotchas (dimension changes require recreation, shadow tables + the substr filter, migration lock + sha256, ANN vs brute-force in 0.1.x, loadable-extension privilege is auto-enabled by better-sqlite3). Amendment B: same-commit rule — reference update lands with the library change.
- [22:56] added `### Migration lock (hand-written preserve-blocks)` section to `docs/DEVELOPMENT.md` right after "Regenerating Drizzle migrations". Explains preserve-markers, what the lock file records, where the enforcement lives (CI verify job + .githooks/pre-commit), the three failure-mode labels, and the `--write` remediation flow. Documents the `pnpm install`-wires-the-hook mechanism via the root `prepare` script.
- [22:57] appended two decision entries to `context_memory/decisions-log.md` — `2026-04-22 22:08 sqlite-vec load failure is strict in test, fail-open in production (S4 refinement)` and `2026-04-22 22:10 pgvector HNSW index parameters are m=16, ef_construction=64`. Both entries carry the full Decision / Rationale / Alternatives considered / Reference structure the existing entries use.
- [23:05] commit `7e94633` on `feat/02-mcp-server` — `feat(db): sqlite-vec extension, pgvector HNSW index, migration lock + pre-commit hook` (22 files, +905/-50). Pre-commit hook fired in real time during the commit, printing `migration-lock: ok (2 blocks verified)` before the commit landed — end-to-end proof the hook is live.
- [16:15 next-day] extended `packages/shared/src/logger.ts` to honour `COODRA_LOG_DESTINATION` at module load: default (unset/stdout) stays stdout; `stderr` routes pino to fd 2 via `pino.destination({ fd: 2, sync: true })`; unknown values throw TypeError at load. Four new tests in `packages/shared/__tests__/unit/logger.test.ts` via `vi.resetModules()` + dynamic import. 64/64 shared tests (was 60, +4). Rebuilt `@coodra/shared` dist so downstream consumers pick up the new export.
- [16:20 next-day] resolved Docker base-image digest on host: `docker pull node:22.16.0-bookworm-slim` + `docker inspect --format='{{index .RepoDigests 0}}' node:22.16.0-bookworm-slim` → `node@sha256:048ed02c5fd52e86fda6fbd2f6a76cf0d4492fd6c6fee9e2c463ed5108da0e34`. Version matches `.nvmrc` (22.16.0). Deliberately NOT alpine per user directive (musl would break better-sqlite3/sqlite-vec prebuilts).
- [16:22 next-day] scaffolded `apps/mcp-server/` workspace: `package.json` (type=module, bin `coodra-mcp-server`, `@modelcontextprotocol/sdk` pinned EXACT at `1.29.0`, `zod@^4.3.6` matching shared, workspace deps on `@coodra/{shared,db}`, tsx devDep), `tsconfig.json` (extends base, composite, src→dist), `tsconfig.typecheck.json` (noEmit, includes __tests__), `vitest.config.ts` (80% thresholds, unit-only include, excludes bootstrap/ from coverage), `README.md` (current scope + invariants), `.env.example` (COODRA_LOG_DESTINATION=stderr canonical).
- [16:24 next-day] wrote `apps/mcp-server/src/bootstrap/ensure-stderr-logging.ts` — side-effect-only module that runs BEFORE any other import of `@coodra/shared`. Sets `COODRA_LOG_DESTINATION=stderr` if unset; refuses to start (writes to stderr + `process.exit(1)`) if explicitly set to anything but `stderr`.
- [16:26 next-day] wrote `apps/mcp-server/src/config/env.ts` — extends `@coodra/shared::baseEnvSchema` with `COODRA_LOG_DESTINATION` (enum, default stderr), `MCP_SERVER_PORT` (coerce to int, default 3100), `LOCAL_HOOK_SECRET` (min-16 optional), `CLERK_{PUBLISHABLE,SECRET}_KEY` (format-regex + sentinel-aware), `CLERK_JWT_ISSUER` (URL). `superRefine` enforces addition C (team-mode Clerk requirements). Exports typed `env` singleton parsed once via `@coodra/shared::parseEnv`. Only module in mcp-server allowed to read `process.env`.
- [16:28 next-day] wrote `src/framework/manifest-from-zod.ts` — wraps Zod v4's native `z.toJSONSchema(schema, { target: 'draft-2020-12', unrepresentable: 'throw' })` with runtime `type === 'object'` check. `zod-to-json-schema` third-party dep NOT installed; decision recorded.
- [16:30 next-day] wrote `src/framework/idempotency.ts` — `IdempotencyKeyBuilder<Input>` contract + `IdempotencyKey` discriminated union (`readonly` | `mutating`) + `assertIdempotencyKeyBuilder` runtime probe that registration calls.
- [16:31 next-day] wrote `src/framework/policy-wrapper.ts` — `PolicyCheck` abstraction + `PolicyDenyError` + `devNullPolicyCheck` S5 stand-in + `logDevNullPolicyInUse()` startup WARN. Designed for single-file swap to `lib/policy.ts::evaluatePolicy` in S7b.
- [16:33 next-day] wrote `src/framework/tool-registry.ts` — core enforcement. `ToolRegistry.register(reg)` validates six invariants synchronously at register time; `assertValid` is public for direct test coverage. `handleCall` routes: input validate → idempotency build → pre policy → handler → output validate → post policy (observational, fire-and-forget). Handlers never see an unwrapped call path. ~410 LOC including docblocks.
- [16:34 next-day] wrote the `ping` tool in three files per §9.1 — `schema.ts` (input capped at 256 chars, strict output), `handler.ts` (pure, round-trips serverTime/sessionId/idempotencyKey), `manifest.ts` (666-char description starting "Call this tool when…", readonly-prefix idempotency builder).
- [16:35 next-day] wrote `src/transports/stdio.ts` using the SDK's low-level `Server` + `setRequestHandler` (NOT `McpServer.registerTool` — the SDK's `@deprecated` on Server means "use McpServer unless you want to own the request lifecycle", which we do). Needed one `as unknown as CallToolResult` cast at the SDK boundary because the SDK's ServerResult union includes a task-result branch our envelope doesn't match.
- [16:36 next-day] wrote `src/index.ts` — first import is `./bootstrap/ensure-stderr-logging.js`, then `env`, then framework, then ping, then stdio. Constructs one registry with `devNullPolicyCheck`, registers ping, boots stdio with `sessionId = stdio:<uuid>`. SIGINT/SIGTERM graceful-shutdown hooks.
- [16:38 next-day] wrote 34 unit tests across 5 files: `manifest-from-zod.test.ts` (4), `tool-registry.test.ts` (13 — 8 negative + 5 happy-path including the "handlers cannot opt out of policy" proof), `config/env.test.ts` (8 fixtures per addition D including the log-destination enum), `tools/ping.test.ts` (8 — manifest contract + roundtrip + oversize rejection + idempotency-key purity), `transports/stdio-stdout-purity.test.ts` (1 — spawns real tsx subprocess, sends `initialize`, asserts stdout is JSON-RPC-only + stderr is pino-JSON-only). First run: 33/34 with one wrong expectation (schema default is `stderr`, not `stdout`); fixed + 34/34 green.
- [16:40 next-day] wrote `apps/mcp-server/Dockerfile` — four-stage build: (1) deps with pnpm+corepack on pinned-by-digest `node@sha256:048ed02c...`, (2) build stage compiles shared → db → mcp-server in dependency order (no turbo cache dependency in the image), (3) `pnpm --filter @coodra/mcp-server deploy --prod --legacy /out` carves out a flat production tree, (4) runtime stage copies /out onto a fresh pinned base as non-root `node` user with `COODRA_LOG_DESTINATION=stderr` + `NODE_ENV=production` in the image. Wrote `.dockerignore` to keep the context tight.
- [16:42 next-day] updated `.mcp.json` — replaced the stub HTTP URL with a real stdio entry: `command=node, args=[apps/mcp-server/dist/index.js], env.COODRA_LOG_DESTINATION=stderr, env.COODRA_MODE=solo`. Comment explains S5 scope + plan for HTTP entry in S16.
- [16:45 next-day] added `### @modelcontextprotocol/sdk (Node.js server)` section to `External api and library reference.md` — exact pin 1.29.0 rationale, Server vs McpServer decision with rationale, code snippet for `setRequestHandler` usage, Zod v4 compatibility note (no `zod-to-json-schema`), stdio transport + logger contract with pointers to all three enforcement points, three specific gotchas. Amended Pino section with the `COODRA_LOG_DESTINATION` env contract note.
- [16:47 next-day] re-sliced `docs/feature-packs/02-mcp-server/implementation.md` S5 — scope grew to absorb previous S6 framework and parts of S7a infra; `ping` walking skeleton now lands in this commit; S6 retired to residual §24.3 assertion helper + system-architecture.md §24.3 amendment only. Deferred list explicitly names every dep NOT installed (`hono`, `@hono/node-server`, `cockatiel`, `@clerk/backend`, `ajv`, `ajv-formats`, `testcontainers`, `@testcontainers/postgresql`).
- [16:50 next-day] appended five S5 decision entries to `context_memory/decisions-log.md`: 2026-04-23 16:20 stdio-only scope, 16:25 COODRA_LOG_DESTINATION env contract + bootstrap module, 16:28 Server-not-McpServer.registerTool, 16:32 drop zod-to-json-schema for Zod v4 native, 16:35 Dockerfile base image digest pin. Each carries Decision / Rationale / Alternatives considered / Reference.
- [20:55 S7a landing] Built `apps/mcp-server/src/framework/tool-context.ts` defining the frozen `ToolContext = ContextDeps & PerCallContext` with interfaces for `DbClient`, `AuthClient`, `PolicyClient`, `FeaturePackStore`, `ContextPackStore`, `RunRecorder`, `SqliteVecClient`, `GraphifyClient`. Wrote nine lib factories under `apps/mcp-server/src/lib/`: `logger.ts` (namespace wrapper over shared), `errors.ts` (`NotImplementedError` + `mcpErrorResult` adapter), `db.ts` (`createDbClient` returning `{ client, asInternalHandle() }` + idempotent close + `_testOverrideInMemory` shorthand), `auth.ts` (`createSoloAuthClient` + `createAnonymousAuthClient` + `SOLO_IDENTITY`), `policy.ts` (`createPolicyClientFromCheck` + `createDevNullPolicyClient` — moved from framework/policy-wrapper.ts), and five domain stubs (`feature-pack`, `context-pack`, `run-recorder`, `sqlite-vec`, `graphify`) whose methods throw `NotImplementedError('<subsystem>.<method>')`. No module-level singletons — every factory is `createXxx(deps)` per user directive.
- [20:55 S7a registry] Refactored `framework/tool-registry.ts`: constructor now `new ToolRegistry({ deps: ContextDeps, clock?, mintRequestId? })`. Handlers receive the full frozen `ToolContext` (previously received a narrow `ToolCallContext`). Policy evaluation routed through `deps.policy.evaluate(...)` in pre + post phases. Registry is the SINGLE place in `src/**` that constructs a `Date` — handlers use `ctx.now()`, which delegates to the injected clock.
- [20:55 S7a ping] Updated `src/tools/ping/handler.ts` — `serverTime = ctx.now().toISOString()` (was `ctx.receivedAt.toISOString()`). Type changed from `ToolCallContext` → `ToolContext` (imported from `framework/tool-context.ts`).
- [20:55 S7a index] Rewrote `src/index.ts` to construct `ContextDeps` from the nine factories, freeze it, hand it to `ToolRegistry`. Shutdown hook closes both the transport and `dbClient.close()` on SIGINT/SIGTERM.
- [20:55 S7a tests] Wrote `__tests__/helpers/fake-deps.ts` (`makeFakeDeps(overrides?)` — single helper for all unit tests that need a `ContextDeps`), `__tests__/unit/tools/_no-raw-date.test.ts` (guard: walks `src/tools/**`, fails CI on literal `new Date(`), migrated `tool-registry.test.ts` + `ping.test.ts` to the new constructor shape via the helper. Added clock-injection test (D) and stable-requestId test (E) to the registry suite. Updated `stdio-stdout-purity.test.ts` to pass `COODRA_SQLITE_PATH=:memory:` so the subprocess doesn't open the user's real `~/.coodra/data.db`. Wrote `vitest.integration.config.ts` + `pnpm test:integration` script + 10 integration test files under `__tests__/integration/lib/` (one per factory, 45 tests total).
- [20:55 S7a lint] Amended `biome.json` with an `overrides` block enabling `suspicious/noImportCycles: 'error'` on `apps/mcp-server/src/lib/**`.
- [20:55 S7a gate] Ran the full gate: `pnpm install --frozen-lockfile` (clean), `pnpm --filter @coodra/db run check:migration-lock` (ok, 2 blocks), `pnpm lint` (0 errors — Biome auto-fixed 11 files for import order + formatting, one pre-existing info-level `useShorthandFunctionType` hint left as-is), `pnpm typecheck` (all 3 packages green), `pnpm --filter @coodra/mcp-server run test:unit` (39/39 — up from 34), `pnpm --filter @coodra/mcp-server run test:integration` (45/45 — new suite), repo-wide `pnpm test:unit` (all packages green via turbo).
- [20:55 S7a docs] Rewrote `docs/feature-packs/02-mcp-server/implementation.md` §S7a to describe what actually landed (frozen `ToolContext` + typed factories + `NotImplementedError` stubs + clock-discipline guard), with the S7b/c deferral list spelled out. Appended the decision entry `2026-04-23 20:55 — S7a: freeze ToolContext shape with typed lib factories` to `context_memory/decisions-log.md` including the Q2/Q3 user-directive pins.

## S7a summary

**Status:** landed locally, gate green, ready to commit.

**Scope:** infrastructure shapes only; S7b lands real auth + policy, S7c lands real feature-pack/context-pack/run-recorder/sqlite-vec/graphify bodies. Tool handlers written against today's `ToolContext` will keep compiling across both swaps.

**Files changed (29):** 1 new framework file (`tool-context.ts`), 9 new lib files (`logger.ts`, `errors.ts`, `db.ts`, `auth.ts`, `policy.ts`, `feature-pack.ts`, `context-pack.ts`, `run-recorder.ts`, `sqlite-vec.ts`, `graphify.ts`), 1 rewritten registry + index pair (`tool-registry.ts`, `index.ts`), 1 trimmed framework file (`policy-wrapper.ts` — devNull moved out), 1 updated handler (`tools/ping/handler.ts`), 1 updated purity test (`stdio-stdout-purity.test.ts`), 1 new test helper (`__tests__/helpers/fake-deps.ts`), 1 new guard test (`__tests__/unit/tools/_no-raw-date.test.ts`), 2 updated unit tests (`tool-registry.test.ts`, `ping.test.ts`), 1 new integration config (`vitest.integration.config.ts`), 10 new integration tests (`__tests__/integration/lib/*.test.ts`), 1 updated `package.json` (script), 1 updated `biome.json` (override), 1 updated `docs/feature-packs/02-mcp-server/implementation.md` (§S7a), 1 updated `context_memory/decisions-log.md` (1 entry).

**Next action:** single commit `feat(mcp-server): S7a — freeze ToolContext + lib factories + clock-discipline guard`.

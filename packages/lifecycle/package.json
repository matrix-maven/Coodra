{
  "name": "@coodra/lifecycle",
  "version": "0.0.0",
  "private": true,
  "description": "Agent-transport-agnostic lifecycle services consumed by the native `lifecycle_event` MCP tool: SessionStart base-SHA capture, stale-run cleanup, kill-switch evaluation, Agent Recipes index injection, and SessionEnd finalization (run completion, run-diff capture, auto Context Pack save, linked Work Pack update).",
  "license": "MIT",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.typecheck.json",
    "lint": "biome check .",
    "test:unit": "vitest run",
    "test:watch": "vitest",
    "clean": "rm -rf dist .turbo"
  },
  "dependencies": {
    "@coodra/db": "workspace:*",
    "@coodra/policy": "workspace:*",
    "@coodra/shared": "workspace:*",
    "drizzle-orm": "^0.45.2"
  },
  "devDependencies": {
    "@types/node": "^22.15.0"
  }
}

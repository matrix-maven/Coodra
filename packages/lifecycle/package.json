{
  "name": "@coodra/lifecycle",
  "version": "0.0.0",
  "private": true,
  "description": "Agent-transport-agnostic lifecycle services — run completion, run diff capture, auto Context Pack save, and linked Work Pack update on SessionEnd. Extracted from apps/hooks-bridge (2026-08-08, Phase 1) so both the HTTP Hooks Bridge and the native-plugin `lifecycle_event` MCP tool get the same SessionEnd behavior instead of only the bridge implementing it.",
  "license": "MIT",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.typecheck.json",
    "lint": "biome check .",
    "test:unit": "vitest run",
    "test:watch": "vitest",
    "clean": "rm -rf dist .turbo"
  },
  "dependencies": {
    "@coodra/db": "workspace:*",
    "@coodra/policy": "workspace:*",
    "@coodra/shared": "workspace:*",
    "drizzle-orm": "^0.45.2"
  },
  "devDependencies": {
    "@types/node": "^22.15.0"
  }
}
